import { pageMetadata } from '@/lib/seo';
import { DocNav } from '@/components/docs/DocNav';

export const metadata = pageMetadata({
  title: '.gitignore と、出してしまった秘密',
  description:
    '.gitignore は「まだ追跡していないファイル」にしか効きません。一度コミットしてしまったものは git rm --cached で外すまで無視されず、外しても 1 つ前のコミットには残ります。',
  path: '/docs/ignore/',
});

# .gitignore と、出してしまった秘密

`.gitignore` でいちばん多い詰まり方は、これです。

> `.gitignore` に書いたのに、まだ `git status` に出てくる。

書き方が間違っているのではありません。**もう追跡しているから**です。

## .gitignore が効く相手は 1 種類だけ

`.gitignore` が効くのは、**まだ一度もコミットされていないファイル**だけです。

| ファイルの状態 | `.gitignore` に書くと |
| --- | --- |
| まだ一度もコミットしていない | 無視される。`git add .` でも入らない |
| すでにコミット済み | **何も起きない**。これまでどおり追跡され続ける |

理由は Git の側から見ると単純です。
`.gitignore` は「これから見つけるファイルのうち、拾わなくていいもの」のリストであって、
**すでに手元に持っているものを手放す**指示ではありません。

手放すには、別のコマンドが要ります。

## 追跡をやめる ― git rm --cached

```bash
git rm --cached .env     # 追跡から外す。ファイルは手元に残る
git rm .env              # 追跡から外し、ファイルも消す
```

ほぼ常に `--cached` のほうです。
`.env` は手元では使い続けるので、消えては困ります。

外したあと、`.gitignore` に書いてあれば、そこからは無視されます。
**順番はどちらでも構いません**が、両方やらないと止まりません。

```bash run
touch .env API_KEY=ひみつ   # うっかり作って
git add .
git commit -m "開店"        # うっかりコミットしてしまった

touch .gitignore
append .gitignore .env      # ← 無視すると決める
git rm --cached .env        # ← 追跡をやめる
git add .gitignore
git commit -m ".env を追跡から外した"
```

## 書き方

このサイトが読むのは 3 つだけです（実務ではこれで大半が足ります）。

```
# コメント。空行と # の行は読み飛ばされます
.env            名前がそのまま一致
*.key           拡張子で一致
secrets/        そのディレクトリの下ぜんぶ
```

本物にはもっと広い書式があります（`!` で打ち消す、途中の `/`、`**` など）。
考え方は同じで、**上から順に当てて、当たったら無視する**だけです。

## いちばん大事なところ ― 1 つ前のコミットには、まだ入っています

`git rm --cached` と `commit` で消えるのは、**これ以降のコミット**からだけです。

```bash
git rm --cached .env
git commit -m ".env を外した"

git diff HEAD~1 HEAD     # ← .env が「消えた側」に出る（HEAD~1 は「1 つ前のコミット」）
                         #    つまり、1 つ前にはまだ入っていたということ
```

コミットは積み重ねであって、上書きではありません。
いまやったのは「`.env` の無いコミットを 1 つ足した」だけで、
その前のコミットは 1 つも書き換わっていません。**中身はまだそこにあります。**

| どこ | `git rm --cached` ＋ コミットのあと |
| --- | --- |
| 手元のファイル | 残っている（`--cached` を付けたので） |
| いちばん新しいコミット | 消えている |
| 1 つ前までのコミット | **残っている** |

### だから、まずやることは「作り直す」です

古いコミットの中だけを書き換える、ということは Git にはできません。
コミットは中身ごと固めて置いてあるので、そこから値を抜くには、
**そのコミットから先を作り直して、別のコミットに置き換える**しかありません。
その作り直しが何をする操作なのかは、ずっと後の[対話的 rebase](/docs/interactive) で扱います。
ここで覚えてほしいのは、そちらではなく**順番**のほうです。

1. **その値を無効にして、作り直す**（API キーの再発行、パスワードの変更）
2. `.gitignore` に足して、`git rm --cached` で追跡から外す
3. 履歴そのものをどうするかは、そのあとで決める

1 が済んでいれば、履歴に残っている値はただの古い文字列です。
順番を逆にすると、掃除している最中もずっと有効なままになります。
そして掃除は、たいてい思ったより長くかかります。

### 他の人と共有していたなら、続きがあります

ここまでは、**あなたの手元にあるリポジトリ 1 つ**の話でした。
そのリポジトリを他の人にも渡してあるなら、値の残っている場所はもっと増えます。
そこは[リモートの記事](/docs/remote)で扱います。

先にやることは、それでも変わりません。**値を作り直すのが 1 番目**です。

## それでも入れたいとき

無視されているファイルを、意図して追跡することもできます。

```bash
git add -f config.local.json
```

`-f` が要るのは、**たいていは間違いだから**です。
本当に必要なときだけ使ってください。

<DocNav id="ignore" />
